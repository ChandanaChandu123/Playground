#include<iostream>
#include<cmath>
using namespace std;
int power(int x)
{
  int y,a,n;
  y=pow(a,n);
}
int main()
{
 int a,n,b,c;
  std::cin>>a>>n;
  b=pow(a,n);
  c=power(b);
  std::cout<<"Enter the value of a"<<"\n"<<"Enter the value of n"<<"\n"<<"The value of"<<" "<<a<<" "<<"power"<<" "<<n<<" "<<"is"<<" "<<c;
  return 0;
}